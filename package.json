{
  "name": "lmcache-leaderboard",
  "version": "1.0.0",
  "description": "LMCache Contributor Leaderboard with automatic updates",
  "type": "module",
  "scripts": {
    "build": "cd dashboard && npm install && npm run build",
    "dev": "cd dashboard && npm run dev",
    "preview": "cd dashboard && npm run preview"
  },
  "dependencies": {
    "@octokit/rest": "^20.1.2",
    "openai": "^6.5.0",
    "tar-stream": "^3.1.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
